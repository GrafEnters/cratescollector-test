# Crates Collector

<table>
<tr>
<td width="50%" valign="top">

https://github.com/user-attachments/assets/345daa13-e9c1-44bb-9c97-8db4b729ace0

</td>
<td width="50%" valign="top">

**Crates Collector** — прототип игры-коллектора предметов с системой инвентаря,  
реализованный на **Unity 6+** с использованием **UI Toolkit**.

Проект демонстрирует:
- работу с предметами в мире
- инвентарь с drag & drop
- архитектурный подход (DI, фабрики, пулы)
- конфигурацию через ScriptableObject и JSON

</td>
</tr>
</table>

---

## 📖 Описание

Игра представляет собой простой симулятор сбора предметов.  
Игрок перемещается по сцене, подбирает предметы и управляет ими через инвентарь с поддержкой drag & drop.

Проект задумывался как **техническое демо**, фокусирующееся на:
- чистой архитектуре
- расширяемости
- читаемости кода
- корректной работе UI Toolkit

---

## ✨ Основные возможности

### 🧍 Персонаж
- Контроллер персонажа с управлением **WASD**
- Камера от третьего лица с настраиваемыми параметрами
- Ограничение движения в пределах платформы

### 📦 Предметы
- Система спавна предметов на сцене (настраиваемое количество)
- Загрузка данных предметов из JSON (`Resources/ItemsData.json`)
- Поддержка **stackable** и **non-stackable** предметов
- Object Pooling для оптимизации (настраиваемый размер пула)
- Подсветка предметов при приближении (outline-эффект)
- Подсказка «Нажмите E» при приближении
- Защита от спавна предметов:
  - слишком близко друг к другу
  - слишком близко к игроку

### 🎒 Инвентарь
- UI-окно с сеткой слотов (по умолчанию 12, настраивается)
- Открытие / закрытие по **Tab**
- Корректное управление курсором при открытом инвентаре
- Drag & drop между слотами
- Выброс предметов обратно в мир:
  - ПКМ
  - перетаскиванием за пределы окна
- Выброс всего стека
- Отображение количества для stackable предметов
- Уведомление о переполнении инвентаря

### ⚙️ Системы
- Dependency Injection контейнер
- Централизованная конфигурация через ScriptableObject
- Система детекции предметов в радиусе
- Визуальная фабрика предметов

---

## 🛠 Технические требования

- **Unity**: 6.0.2.12f1 или выше  
- **UI**: UI Toolkit (UXML, USS)  
- **Язык**: C#  
- **Input**: Unity Input System  

---

## 📁 Структура проекта
```
Assets/
├── Scripts/
│   ├── Core/              # Ядро системы (DI, конфигурация, фабрики)
│   ├── Data/              # Модели данных (ItemData, InventorySlot, MainGameConfig)
│   ├── Inventory/         # Логика инвентаря
│   ├── Items/             # Логика предметов (спавн, подсветка, сбор)
│   ├── Player/            # Контроллер игрока и камера
│   └── UI/                # UI компоненты (инвентарь, подсказки, уведомления)
├── Resources/
│   ├── ItemsData.json     # Данные предметов
│   ├── InventoryWindow.uxml  # UI шаблон инвентаря
│   ├── InventoryWindow.uss  # Стили инвентаря
│   └── MainGameConfig.asset  # Конфигурация игры
├── Scenes/
│   └── CoreScene.unity    # Основная сцена
├── Shaders/
│   ├── Outline.shader
│   └── OutlinePostProcess.shader
└── Other/
    └── InputSystem_Actions.inputactions
```


---

## ▶️ Установка и запуск

1. Клонируйте репозиторий
2. Откройте проект в **Unity 6.0.2.12f1** или новее
3. Дождитесь импорта всех ассетов
4. Откройте сцену `Assets/Scenes/CoreScene.unity`
5. Нажмите **Play**

---

## 🎮 Управление

- **WASD** — движение персонажа  
- **Tab** — открыть / закрыть инвентарь  
- **E** — подобрать предмет  
- **ЛКМ** — drag & drop в инвентаре  
- **ПКМ** — выброс предмета (один или весь стек)  

---

## ⚙️ Настройка

Основные параметры настраиваются через `MainGameConfig`:

- Параметры игрока (скорость движения, поворота)
- Инвентарь (количество слотов, блокировка обзора)
- Спавн предметов:
  - количество
  - радиус
  - минимальные расстояния
  - высота и масштаб
  - максимальное число попыток
- Object Pool (размер пула)
- Камера (дистанция, высота, углы, скорость)
- Подбор предметов (дистанция, высота подсказки)
- Подсветка (цвет, толщина outline)

### Данные предметов (`ItemsData.json`)
- ID
- Название
- Цвет (RGBA)
- Stackable (да / нет)
- MaxStack

---

## 🧱 Архитектура

Проект использует следующие паттерны и подходы:

- **Dependency Injection** — `DIContainer`
- **Factory Pattern** — `IItemFactory`, `ItemVisualFactory`
- **Object Pooling** — `ItemPool`
- **State Provider** — `IInventoryStateProvider`
- **Configuration Pattern** — `MainGameConfig`
- **Interface Segregation** — `IItemDetector`, `IItemOutlineManager`, `IConfigProvider`

---

## 🔧 Расширение функциональности

### Добавление нового предмета
1. Добавьте запись в `Resources/ItemsData.json`
2. Предмет автоматически появится в пуле спавна

### Изменение количества слотов инвентаря
1. Измените `InventorySlotCount` в `MainGameConfig`
2. При необходимости обновите UXML-шаблон
